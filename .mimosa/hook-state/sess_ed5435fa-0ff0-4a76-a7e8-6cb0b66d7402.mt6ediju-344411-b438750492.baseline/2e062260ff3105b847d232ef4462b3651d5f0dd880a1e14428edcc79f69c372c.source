# rarecruise/www/set-password.py
# Set Password page controller

no_cache = 1
allow_guest = True


def get_context(context):
    context.no_cache = 1
    # Token dibaca oleh JS dari URL query string
    # Tiada server-side check di sini — semua handled by set-password.js
