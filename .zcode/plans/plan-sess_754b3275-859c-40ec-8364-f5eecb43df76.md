Tambah badge "With Flight" dalam `rc-sched-side` pada kad `rc-sched-card` di /cruise-schedule — dipaparkan bila mana-mana variant TGD pada kad ada pakej penerbangan (pakej dengan `airport_form`, group bukan cruise-only).

**Perubahan (3 fail, tiada query baru):**

1. `travel_booking/www/cruise_schedule.py` — dalam `_attach_booking_options()` (loop `for e, pairs in wanted:`), kira flag untuk SETIAP kad:
```python
e["has_flight"] = any(
    p.get("flight") and not p.get("is_cruise_only")
    for _ev, pkgs in pairs for p in pkgs
)
```
Data pakej sudah preloaded (`trip_packages`), jadi tiada query tambahan.

2. `travel_booking/www/cruise-schedule.html` — selepas blok `rc-sched-price` (baris 147), sebelum badge seats:
```html
{% if e.has_flight %}
<span class="rc-sched-flight"><i class="ti ti-plane-takeoff"></i> With Flight</span>
{% endif %}
```

3. `travel_booking/public/css/cruise_schedule.css` — gaya pill sebelah peraturan `.rc-sched-seats`, ikut corak sedia ada (11px/600, radius 20px) dengan ton emas tema:
```css
.rc-sched-flight {
  display: inline-flex; align-items: center; gap: 4px;
  width: max-content; padding: 3px 10px; border-radius: 20px;
  font-size: 11px; font-weight: 600;
  background: #FFF6DE; color: #8C6D1F;
}
.rc-sched-flight i { font-size: 13px; }
```

**Verifikasi:** `bench build --app travel_booking` (CSS) + reload bench (Python), kemudian semak /cruise-schedule — kad dengan variant Fly Cruise papar badge "With Flight" selepas harga; kad cruise-only tulen tiada badge.