## Buang map_lat dari Trip

### Apa yang akan dibuat
Hapus dead code berkaitan peta dari `travel_booking/travel_booking_management/doctype/trip/trip.py`:

**Baris untuk dibuang (191-195):**
```python
# Location map (lat/lng pada Trip). has_map = False kalau tiada koordinat.
context.map_lat = float(self.map_lat or 0)
context.map_lng = float(self.map_lng or 0)
context.map_zoom = int(self.map_zoom or 8)
context.has_map = bool(self.map_lat and self.map_lng)
```

### Mengapa selamat
- Field `map_lat`, `map_lng`, `map_zoom` tidak wujud dalam DocType JSON Trip
- Nilai context ini tidak digunakan oleh mana-mana template atau JavaScript
- Ini adalah sisa feature peta yang tidak siap (dead code)

### Fail yang diubah
- `travel_booking/travel_booking_management/doctype/trip/trip.py` — buang 5 baris